# ピピトリ ソース全体点検メモ

## 点検対象
- `server.js`
- `public/index.html`
- 既存の監査メモ類

## 実施した確認
- Node.js 構文チェック: `server.js` OK
- `index.html` 内 JavaScript 抽出後の構文チェック OK
- WebSocket 簡易スモーク確認: 部屋作成、CPU追加、ゲーム開始、CPU進行、人間手番のカード送信、ピック候補選択、CPUピック進行を確認
- ルール整合性の再点検: マストフォロー、ババブタ、マッド・ピッグ、ピック、ペア浄化、ラウンド終了条件、次ラウンド補充、CPUコメント/UI表示

## 修正した主な点

### 1. ラウンド終了条件の矛盾修正
- 以前は「手番開始時の現在プレイヤー」だけを見ていたため、別プレイヤーがババブタ1枚だけになった場合に検知が遅れる可能性がありました。
- `findRoundEndPid()` と `roundEndReason()` を追加し、誰かが以下の条件を満たしたら正しくラウンド終了するよう修正しました。
  - 手札0枚
  - ババブタ1枚だけ
- トリック中は即終了せず、トリック4枚解決後・結果確認後に終了する流れを維持しました。
- ピック後やペア浄化後にも、全プレイヤーの終了条件を確認するよう修正しました。

### 2. トリック途中の終了判定による停止リスクを修正
- トリックが進行中の状態で `maybeEndAtTurnStart()` がラウンド終了させないよう、`activeTrickInProgress()` を使って保護しました。
- カードを出した結果、そのプレイヤーの手札が0枚またはババブタ1枚だけになった場合は、トリック解決後に終了判定へ進むようにしました。

### 3. マッド・ピッグOFF時の表示矛盾を修正
- `publicCard()` と `cardText()` にルーム設定を渡せるようにし、マッド・ピッグOFF時にどろんこ11が `MAD` 表示や「マッド・ピッグ」表記にならないよう修正しました。
- 手札、場札、ピック結果、開始時ペア捨て、ログ表示、CPUセリフ用カード名の整合性を改善しました。

### 4. CPUセリフ判定の文脈ズレを修正
- CPUがカードを出した後にセリフを判断していたため、リード時・勝ちに行った時の判定がずれることがありました。
- カードを出す前のリードスート・場の最高値を保持し、それを基準にセリフを選ぶよう修正しました。

### 5. UIレイアウトの安定化
- スマホ縦向き、横向き、PC表示で、固定値に依存していた領域を再点検しました。
- `syncLayoutMetrics()` により、topbar / scoreBoard / handDock の実測値をCSS変数に反映し、卓エリアの位置計算に使うようにしました。
- scoreBoard の高さや handDock の高さに応じて、場札エリアが重なりにくくなるよう調整しました。
- ROOM表示・ロゴ・ターン順表示が狭い画面で詰まりにくいよう最大幅と省略表示を強化しました。

### 6. 静的ファイル配信の安全性改善
- `serveStatic()` のパスチェックを `path.resolve()` ベースに変更し、意図しないパス参照をより安全に防ぐよう修正しました。

## 残している仕様上の挙動
- 次ラウンド補充では、現在手札と最初に抜いたカードは重複しないようにしています。
- 得点パイルやペア浄化済みカードは履歴として扱い、次ラウンド補充用の通常カードのカードフェイス再利用は許容しています。カード総数を厳密に物理カード1セットとして扱うと、複数ラウンドで補充不能になるためです。
- CPUは公開ピック時、候補カードの中身を見ずに候補位置から選びます。

## 構文チェック
- `node --check server.js` OK
- `index.html` の `<script>` 抽出後 `node --check` OK

- 2026-07-05: 裏向きカードの共通デザインを変更。紫の S デザインを廃止し、赤系ストライプ + 🐽 アイコンへ統一。公開ピック待機・候補表示など、`cardEl({back:true})` を使う全箇所へ一括反映。

## 2026-07-05 追加全体デバッグ点検 v3
- 公開ピックで、準備時間中にクリックするとサーバー側で無視され、ユーザー視点では止まったように見える問題を修正。
  - サーバー側：早すぎる doPick は `delayedPick` タイマーで準備完了後に実行するフェイルセーフを追加。
  - フロント側：準備中の裏向きカードを一時的に disabled 表示にして、準備完了後に自動で再描画。
- フェーズ切り替え時に古いカード選択状態が残り、別画面で選択済み表示が残る可能性を修正。
- 進行系タイマーのクリア対象に `delayedPick` を追加。
- `disabledCard` を pointer-events 無効化し、押せない状態が見た目と挙動で一致するよう調整。

## 2026-07-05 追加全体点検 v4
- 進行停止対策として、サーバー側に1秒間隔の軽量 watchdog を追加。
  - トリック結果確認タイマーが何らかの理由で進まなかった場合、`trickReview.until` 経過後に自動でピック処理へ進めます。
  - ピック結果表示後に `pickFinish` タイマーが失われた場合も、一定時間後に次リードへ進めます。
- 公開ピックの準備時間中にユーザーがカードを押した場合、クリックを無視して止まったように見える問題を修正。
  - サーバー側は準備完了後に delayedPick として実行。
  - フロント側は準備中カードを disabled 表示し、準備完了時に自動再描画します。
- フェーズ切り替え時に古い選択状態が残る可能性を修正。
- 静的ファイル配信で不正なURLエンコードが来てもサーバーが落ちないよう、`decodeURIComponent` を try/catch 化。
- `server.js` と `public/index.html` 内 JavaScript の構文チェックを実施。

## 追加監査 v5
- ピック結果省略時に進行が止まったように見える問題を重点点検。
- `finishingPick` のタイマー消失に対する watchdog を強化。
- ペア浄化待機UI、選択状態リセット、人間アイコン割り当てを修正。

- 2026-07-05: UI監査 v6。スマホ縦横/PCの表示チラつき原因になりやすい全量再描画を減らし、状態変化がない限り DOM を維持するよう修正。


## 2026-07-05 PC UI視認性追加監査
- PC表示で、出札アイコンとカードの重なり、CPUコメントの重なり、scoreBoardスクロール発生を確認対象として修正。
- 変更はPC幅向けメディアクエリに限定し、スマホ縦/横のレイアウトとの整合性を維持。


## 2026-07-05 全体デバッグ点検 v7
- 切断中の人間プレイヤーが手番・3枚パス・開始時ペア捨て・ピック候補選択・公開ピック・ペア浄化待ちに入った場合、ゲーム進行が止まる可能性を修正。
- 切断中の人間プレイヤーは一定時間後にCPU相当の自動処理へフォールバックするようにした。
- ピックやシュート演出がサーバー側で期限切れになった後、クライアント上で古いオーバーレイが残る可能性を修正。
- ラウンド継続時に `lastPickReveal` / `shootEvent` を明示的にクリアし、次ラウンドへ古い演出が残らないようにした。


## 2026-07-05 UI表示安定化 v8
- スマホ縦向き・スマホ横向き・PCブラウザ表示で、表示の揺れやチラつきが起きやすい箇所を追加点検。
- CPUコメント欄を常時確保するようにし、コメントが出たり消えたりした時にプレイヤー欄の高さが跳ねる問題を軽減。
- `resize` / `orientationchange` 時の再描画を即時連打ではなく短時間デバウンスし、スマホ回転時のちらつきを抑制。
- 画面回転時は古い出札アイコン位置がズレて残らないよう、出札アイコンをいったんクリア。
- `prefers-reduced-motion` 環境で出札アイコンの消去アニメーションが発火せず、アイコンが残る可能性をフォールバックタイマーで修正。
- `scoreBoard` / `tableArea` / `handDock` にレイアウト安定化用の containment と scrollbar-gutter を追加。
